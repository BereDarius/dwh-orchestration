version: "3.8"

services:
  prefect-server:
    image: prefecthq/prefect:3-latest
    container_name: prefect-server
    command: prefect server start --host 0.0.0.0 --port 4200
    ports:
      - "4200:4200"
    environment:
      - PREFECT_SERVER_API_HOST=0.0.0.0
      - PREFECT_API_DATABASE_CONNECTION_URL=postgresql+asyncpg://prefect:prefect@postgres:5432/prefect
    volumes:
      - prefect-data:/root/.prefect
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - prefect-network

  postgres:
    image: postgres:15-alpine
    container_name: prefect-postgres
    environment:
      - POSTGRES_USER=prefect
      - POSTGRES_PASSWORD=prefect
      - POSTGRES_DB=prefect
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prefect"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - prefect-network

  prefect-worker-dev:
    build:
      context: ..
      dockerfile: deployment/docker/Dockerfile
    container_name: prefect-worker-dev
    command: python -m orchestration.deploy --serve --environment dev
    environment:
      - PREFECT_API_URL=http://prefect-server:4200/api
      - ENVIRONMENT=dev
    volumes:
      - ../config:/app/config:ro
      - ../data:/app/data
      - worker-logs:/app/logs
    depends_on:
      - prefect-server
    restart: unless-stopped
    networks:
      - prefect-network

  # Uncomment for production worker
  # prefect-worker-prod:
  #   build:
  #     context: ..
  #     dockerfile: deployment/docker/Dockerfile
  #   container_name: prefect-worker-prod
  #   command: python -m orchestration.deploy --serve --environment prod
  #   environment:
  #     - PREFECT_API_URL=http://prefect-server:4200/api
  #     - ENVIRONMENT=prod
  #   volumes:
  #     - ../config:/app/config:ro
  #     - ../data:/app/data
  #     - worker-logs:/app/logs
  #   depends_on:
  #     - prefect-server
  #   restart: unless-stopped
  #   networks:
  #     - prefect-network

volumes:
  prefect-data:
  postgres-data:
  worker-logs:

networks:
  prefect-network:
    driver: bridge
